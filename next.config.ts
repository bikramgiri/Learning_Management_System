import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "bootstrapmade.com", "lh3.googleusercontent.com", "shorturl.at"],
  },
};

export default nextConfig;
